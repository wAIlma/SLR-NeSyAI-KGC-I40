PREFIX bibo: <http://purl.org/ontology/bibo/>
PREFIX cito: <http://purl.org/spar/cito/>

SELECT ?paper (COUNT(?citation) AS ?citationCount)
WHERE {
    ?citation cito:cites ?paper .
    ?paper a bibo:Document .
}
GROUP BY ?paper
ORDER BY DESC(?citationCount)
